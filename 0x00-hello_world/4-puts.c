
#include<stdio.h>

/**
 * main - Entry point
 * Description: 'the use of puts()'
 * Return: Always 0 (Success)
 */
int main(void)
{
char str[] = "\"Programming is like building a multilingual puzzle";

puts(str);

return (0);

}
